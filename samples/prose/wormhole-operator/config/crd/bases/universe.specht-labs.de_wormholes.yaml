---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: wormholes.universe.specht-labs.de
spec:
  group: universe.specht-labs.de
  names:
    kind: Wormhole
    listKind: WormholeList
    plural: wormholes
    singular: wormhole
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.charge
      name: Charge
      type: integer
    - jsonPath: .spec.destination
      name: Destination
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Wormhole is the Schema for the wormholes API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WormholeSpec defines the desired state of Wormhole.
            properties:
              destination:
                description: |-
                  Destination is where the far mouth opens. Cosmetic; it only ever flows into
                  a ConfigMap and a coordinate reservation.
                type: string
              paused:
                default: false
                description: Paused stops reconciliation when true. It drives the
                  skip gate.
                type: boolean
              relayRef:
                description: RelayRef is the name of the cluster-scoped SubspaceRelay
                  to route through.
                type: string
              throughput:
                default: 0
                description: |-
                  Throughput is the requested traffic units. When greater than zero and the
                  tunnel is open, the reconcile routes traffic through the referenced relay.
                format: int32
                minimum: 0
                type: integer
            type: object
          status:
            description: WormholeStatus defines the observed state of Wormhole.
            properties:
              charge:
                description: Charge climbs toward the ignition threshold (88) over
                  successive reconciles.
                format: int32
                type: integer
              conditions:
                description: Conditions follow the standard Kubernetes condition convention.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              coordinates:
                description: Coordinates is the reserved subspace coordinate id.
                type: string
              entryAnchor:
                description: EntryAnchor and ExitAnchor name the two owned Anchor
                  resources.
                type: string
              exitAnchor:
                type: string
              linkSession:
                description: LinkSession is the id of the most recent subspace link.
                type: string
              phase:
                description: Phase is a coarse human-facing lifecycle marker.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
