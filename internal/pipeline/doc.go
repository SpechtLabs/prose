// Package pipeline is the generic core of prose: the step and group tree, the
// depth-first executor, the typed reconcile Context, the Builder, and the
// controller-runtime runner. The public github.com/spechtlabs/prose/pkg/prose
// package is a thin facade over it.
package pipeline
